class Solution {
    public int minMoves(int[] nums) {
   Arrays.sort(nums);
int n=nums.length;
        int d=nums[0];
        int sum=0;
        for(int i=1;i<n;i++){
       sum=sum+(nums[i]-d);   
        }
        return sum;
    }
}
