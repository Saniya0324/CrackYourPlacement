class Solution {
    public List<Integer> getRow(int rowIndex) {
   ArrayList<Integer> a=new ArrayList<>();
     a.add(1);
    for(int i=1;i<=rowIndex;i++){
    long prev=(long) a.get(i-1);
    long cur=prev*(rowIndex-i+1)/i;
        a.add((int) cur);
    }
        return a;
    }
}
